pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'lalala'
                sh 'git clone git@github.com:tingjieli/first.git'
            }
        }
    }
}
