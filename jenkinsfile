pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello Worldddddddddddd'
                sh 'git clone git@github.com:tingjieli/first.git'
            }
        }
    }
}
