pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello Worldddd'
                sh 'git clone git@github.com:tingjieli/first.git'
            }
        }
    }
}
